
services:
  dev:
    image: oven/bun:latest
    container_name: ts-clock-app-dev
    user: "1000:1000"
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "3000:3000"
      - "8000:8000"
    stdin_open: true
    tty: true
    command: bash